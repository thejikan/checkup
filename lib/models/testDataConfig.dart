class TestDataConfig {
  String title;
  int tests;
  double amount;
  double discount;
  int id;

  TestDataConfig({
    required this.title,
    required this.tests,
    required this.amount,
    required this.discount,
    required this.id,
  });
}
